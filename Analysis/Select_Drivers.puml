@startuml
left to right direction
skinparam backgroundColor white

actor "User" as User
boundary "Driver Selection View" as GUI
control "Driver Controller" as CTRL
entity "Driver List" as DRIVERS
entity "Active Session" as SESSION

User --> GUI : select drivers
GUI --> CTRL : submit selection / request list
CTRL --> SESSION : get current session
CTRL --> DRIVERS : load available drivers
CTRL --> DRIVERS : validate selected drivers
CTRL --> GUI : show confirmation / errors
@enduml
